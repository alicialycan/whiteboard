'use strict';
// Write helper functions
// Traverse through tree to find values
// Push 'good' values into accumulator array

hasBoth(currentNode, val1, val2); 
contains(val1) && contains(val2);

getPath(val1);

path1 = [8, 6, 7];
path2 = [8, 6, 9, 5];